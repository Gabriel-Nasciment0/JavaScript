var vel = 80;
console.log(` A velocidade do seu carro e ${vel} KM/H`);
if (vel >= 60) { // se existe apenas um if (se) e condição simples 
    console.log(`Voce passou a velocidade pemitida foi multado`);
} else { // se houver um else(se não) e condição composta
    console.log(`dirija sempre com o cinto de segurança`);
}
